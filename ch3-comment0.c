/* this is to show how to comment */
#include <stdio.h>
main()


{
    // int i=0; 
    for (int i=0; i<10; i++)
    {                           /* a curly braces must exist, if not it will make error */
    printf("%d\n",i);           /* data as double/integer, new line */
    }
    return 0;
}
